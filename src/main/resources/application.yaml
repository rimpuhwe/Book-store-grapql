spring:
  application:
    name: Book

  datasource:
    url: jdbc:postgresql://localhost:5432/Bookstore_Management_System
    password: raissamich12@gmail.com
    driver-class-name: org.postgresql.Driver
    username: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true


  graphql:
    graphiql:
      enabled: true
    schema:
      introspection:
        enabled: true
  flyway:
    baseline-on-migrate: true